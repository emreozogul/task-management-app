import { SettingsCard } from './SettingsCard';

export default function ResetData() {
    return (
        <SettingsCard
            title="Reset Data"
            description="Reset all data and start fresh"
        >
            <div>ResetData</div>
        </SettingsCard>
    );
}
